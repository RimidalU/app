// import { AttributesModel, BaseComponent } from '../BaseComponent/baseComponent';

// export class Button extends BaseComponent {
//   constructor(Btntext: string, className: string, params: AttributesModel[] = []) {
//     super('button', ['button', `${className}`], params);
//     this.element.innerHTML = Button.addButtonText(Btntext);
//   }

//   static addButtonText(Btntext: string): string {
//     return `${Btntext}`;
//   }
// }
